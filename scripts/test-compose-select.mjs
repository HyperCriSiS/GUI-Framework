// SPDX-License-Identifier: AGPL-3.0-or-later
import assert from "node:assert/strict";
import { readFile } from "node:fs/promises";
const source = await readFile("packages/adapter-compose/src/main/kotlin/GuiSelect.kt", "utf8");
assert.match(source, /data class GuiSelectOption\(/); assert.match(source, /fun GuiSelect\(/); assert.match(source, /options: List<GuiSelectOption>/); assert.match(source, /value: String/); assert.match(source, /onValueChange: \(String\) -> Unit/); assert.match(source, /query: String = ""/); assert.match(source, /onQueryChange: \(String\) -> Unit = \{\}/); assert.match(source, /editable: Boolean = false/); assert.match(source, /expanded: Boolean = false/); assert.match(source, /TextFieldValue\(text = query, selection = TextRange\(query\.length\)\)/); assert.match(source, /editingValue\.composition != null/); assert.match(source, /editingValue\.text == query/); assert.match(source, /editingValue\.composition == null/); assert.match(source, /value = editableFieldValue/); assert.match(source, /editingValue = nextValue/); assert.match(source, /onQueryChange\(nextValue\.text\)/); assert.match(source, /if \(editable && editableFieldValue\.composition != null\) return@onPreviewKeyEvent false/); assert.match(source, /BasicTextField\(/); assert.match(source, /PopupPositionProvider/); assert.match(source, /Popup\(/); assert.match(source, /PopupProperties\(focusable = !editable\)/); assert.match(source, /Key\.DirectionDown/); assert.match(source, /Key\.DirectionUp/); assert.match(source, /Key\.Enter, Key\.NumPadEnter/); assert.match(source, /Key\.Escape/); assert.doesNotMatch(source, /androidx\.compose\.material/); assert.doesNotMatch(source, /animate[A-Z]|Animated/);
console.log("Compose Basic Select / ComboBox source contract tests passed.");
